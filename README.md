# React Review

- [ ] new React App
- [ ] React basics (components, jsx, props)
- [ ] controlled components (input, list)
- [ ] handle events (buttons)
- [ ] data fetching (useEffect, axios)
- [ ] connecting an API project

This repository contains 2 distinct projects:

1. React Front End project
2. Express Back End project

Note that these are two **completely separate** software projects and should even be opened in 2 different VSCode Windows. They are also installed and run separately